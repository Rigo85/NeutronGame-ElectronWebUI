'use strict';

const Q = new require('Q')();

Q.load('Q.json');

exports.move = board => {
    //calcular el stado del board
    //buscar el mayor en sus conexiones
    //buscar de los posible mov. cuales dan ese estado(indefinido)
    ////en caso de no presnetar ese estado, aplicar el mov que mas valor obtenga
    
}
